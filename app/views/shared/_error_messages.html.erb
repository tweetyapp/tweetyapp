<% if object.errors.any? %>
<div id="error_explanation">
	<h2>
		<%= pluralize object.errors.count, "error" %>
		prohibited this operation from being done<!--<%= object.class.to_s.underscore.humanize.downcase %> from being saved: -->
	</h2>
	<p>
		The were problems with following fields 
	</p>
	<ul>
		<% object.errors.full_messages.each do |msg| %>
		<li> <%= msg %></li>
		<% end %>
	</ul>

</div>

<% end %>